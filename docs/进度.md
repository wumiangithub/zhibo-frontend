# 前端进度

架构/需求/API 以 `../zhibo-shared/docs/` 为准。后端进度在 `../zhibo-backend/docs/进度.md`。

## 当前状态

- **第四迭代已验收**：观看页 JSSDK 3.9.1；预告倒计时 / 结束态 / `live_over`；点击收听。
- **第五迭代代码已齐**（T11.0～T11.5）：布局 / 聊天+假验证 / 公告 / 开播弹窗 / 点赞·在线·分享·礼物占位；另含弹幕 tab、表情、WatchView 拆分。
- **第六迭代**：`/live/create` UI + 真接口已接（仅 `title/startTime/type`）。**等 T12.0b 契约落定后再做 T12.2 / T12.3**（勿先扩校验/假传字段）。
- **直播管理详情**：列表「管理」进 `/live/:id`（默认开播设置）；二级菜单含基本设置 / 开播设置 / 运营设置 / 直播数据。管理端前端路由统一走 `/live*`，已去掉旧 `/activities` 页面与重定向。
- **另**：T11.7 真实开播联调仍待各仓。
- T6.4～T9.7：统计、运营改删、观看端列表等均已完成。

## 任务

- [x] T0.3 admin-web 脚手架
- [x] T0.4 live-web 脚手架
- [x] T0.5 UnoCSS 验证（两端 `npm run dev` 原子类生效）
- [x] T5.1～T5.3 Axios / 路由 / 布局 / API
- [x] T6.1 `/watch/:id` 嵌入页
- [x] T6.2 活动列表 / 创建 / 详情 + 开播
- [x] T6.3 详情内嵌管理后台预览
- [x] T6.4 统计页
- [x] T7 联调（真开播画面已验收）
- [x] T8.3 入口与观看链接
- [x] T8.5 统计页联调
- [x] T9.5 管理端运营操作（编辑/结束/删除）
- [x] T9.6 观看端列表+昵称
- [x] T9.7 第三迭代联调
- [x] T10.2 / T10.3 观看页接 SDK + 错误/嵌入兜底
- [x] T10.6 第四迭代联调
- [x] T10.8 预告倒计时 / 结束态
- [x] T10.9 结束态改 `live_over`（已删状态轮询）

## 阻塞项

- 本机 Vite 只监听 `localhost`（IPv6 `::1`），用 `http://127.0.0.1:5173` 会连不上，请用 `http://localhost:5173`。
- **管理端工作台首页**：待办事项、经营数据暂无后端接口，页面用假数据占位；近期直播目前也是 Mock（样式优先），后续应接 `GET /api/activities`。
- **新建直播页（`/live/create`）已接真接口**：`onSave` 调 `POST /api/activities`，当前只传 `title / startTime / type`（后端现有字段）。mode→type 映射：视频=2 / 语音=1 / **录播=4（点播，后端待支持）**。封面/暖场/宣传封面/富文本详情/角色/回放/归属人/所属课程/商品信息等字段，等后端扩字段后再补。
- **T12.2 / T12.3 阻塞**：依赖文档 **T12.0b**（`API约定.md` POST `/api/activities` 请求字段表、校验、录播策略）。当前契约仍只有 `title/startTime/type`，落定前不动前端对齐。

## 变更日志

- 2026-09-24：去掉全部 `/activities` 前端路由与重定向，只保留 `/live`、`/live/create`、`/live/:id`（后端 API `/api/activities` 不变）。
- 2026-09-24：删除废弃活动列表页 `views/activity/ListView.vue`；`/activities` 重定向到 `/live`。
- 2026-09-24：直播管理「管理」改为 `/live/:id` 详情壳（默认开播设置；基本设置复用新建表单+活动信息；运营设置保留原运营操作；直播数据迁入原统计页）；顶栏去直播/分享接真逻辑，其余 toast；去掉直播计划步骤条；旧 `/activities/:id`、`/stats` 重定向兼容。
- 2026-09-23：接手第六迭代——确认 T12.0 / T12.0b / T12.1 均为待办；前端 **等 T12.0b 契约落定后** 再做 T12.2（校验与入参）/ T12.3（成功跳转 + 占位说明）。
- 2026-09-23：主题色抽到 `admin-web/src/styles/theme.css`（CSS 变量 + `data-theme` 可切换）；admin-web 品牌色改为变量；约定写入 `docs/设计规范.md`，`前端协作.md` 已挂链接。
- 2026-09-23：富文本「插入」做成截图式分组下拉（插入素材：图片/音频/视频/文档；布局和样式：表格/超链接/引用/分隔线），嵌进工具栏最左；上传类点击提示「待接入」，表格/超链接/引用/分隔线接通。直播详情最大字数改为 2000。
- 2026-09-23：富文本编辑器默认高度改 500px，加字数统计（`editor.getText().length` 实时计算，上限 100000，footer 显示「已输入X个字符，还可以输入Y个字符」+ 左侧友情提示）；「查看示例」移到编辑器上方右上角。
- 2026-09-23：废弃旧创建页 `/activities/create`（删 `CreateView.vue` + 路由），所有入口（工作台/活动列表/直播管理/侧栏）跳转改到 `/live/create`。新页 `onSave` 接真接口 `POST /api/activities`，当前只传 `title/startTime/type`（mode→type：视频2/语音1/录播4点播），其余字段等后端扩。
- 2026-09-23：新建直播页 `/live/create` 落地（参考小鹅通）。引入 Element Plus（按需）+ WangEditor；表单按截图拆 8 个 section + 手机预览 + 通用 FormRow/ImageUploader/RichTextEditor；保存暂为 Mock。录播直播模式 = 微吼点播，已记入阻塞项待后端扩接口。
- 2026-09-22：封装 `UiButton` / `UiInput` / `UiCheckButton`（对齐参考图：实心蓝、蓝框、灰框、文字链），直播管理页改用组件。
- 2026-09-22：顶栏「直播」→ `/live` 直播管理页（接 `GET /api/activities`）；直播模块侧栏（新建/管理/数据等）；壳操作 toast，去直播/管理/数据接现有能力。
- 2026-09-22：协作约定「问清再干」写入 `AGENTS.md` / `docs/前端协作.md`——有不清楚先问用户，问清再动手；无疑问则直接干。
- 2026-09-22：admin-web 工作台首页壳——顶栏「钱坤云直播平台」+ 侧栏；`/` 为概况（近期直播/待办/经营数据 Mock）；「全部直播」进原活动列表；未开放菜单 toast。
- 2026-09-22：20005 偶发且直播未结束——按可重试处理（自动重拉签名再 init，最多 2 次）；文案改为「活动异常」避免误解成一定已结束。硬失败仍立刻提示。
- 2026-09-22：观看页「播放器加载失败」对照控制台：微吼 SDK **20005 活动不存在或已结束** 后内部 watermark/marquee 崩掉，`<video>` 建不出来；本站 `/sdk` 仍可能报 state=1。已把致命错误抬到遮罩，并提供刷新/嵌入兜底；顺手防 `interactTools` 空指针。
- 2026-09-22：修复观看页「点击播放」无反应——根因是 `<video>` 未就绪时就出遮罩，点击后 await 等待会丢掉用户手势，浏览器静默 NotAllowedError；改为等媒体就绪再出按钮，并区分加载中/失败态。与 node_modules 无关（微吼 SDK 走 CDN）。
- 2026-09-21：`WatchView.vue`（1891 行）按功能拆分，行为不变——`composables/`（useChat / useBarrage / useRoomStats / usePlayerControls / useCountdown / useAnnouncement / useEmojiInsert）、`components/watch/`（PlayerStage / ChatPanel / BarragePanel / BarrageLayer / PreviewScreen / EndedScreen / NicknameForm）、`utils/sdk.ts`（脚本加载 + 错误码文案）；页面只保留状态与编排（450 行），最大单文件 450 行。
- 2026-09-21：聊天与弹幕支持表情——`components/EmojiPanel.vue` + `constants/emoji.ts`（90 个微吼表情）；`[令牌]` 与微吼线上回传的 `<img>` HTML 都渲染成图片（src 白名单，其余一律转义，不用 v-html）；乐观上屏与服务端回声按归一化文本去重；发送成功与历史加载完成后消息列表滚动到底。
- 2026-09-20：弹幕传输改走聊天通道（微吼文档：openBarrage 后用发送聊天接口发弹幕、收到聊天也显示为弹幕）；前端浮层只负责本地滚动样式与设置，发送调用 `chat.sendChat`，回声按内容去重避免弹幕/聊天重复。
- 2026-09-20：弹幕改为前端自建（SDK 渲染器在本布局不绘制、设置触发器 `.v-c-right` 被压成 0 宽不可达）；侧栏新增独立「弹幕」tab（开关/显示区域/不透明度/字体/颜色/速度 + 独立输入）；修复发聊天本地出现两条（乐观占位与回声按内容合并）。
- 2026-09-20：第四迭代验收——T10.2/T10.3/T10.6/T10.8/T10.9 勾选完成。
- 2026-09-18：完成 T9.5 管理端运营操作（编辑标题/时间、结束直播、删除活动）与 T9.6 观看端（首页活动列表、昵称输入）；T9.7 总控真实凭证联调通过。
- 2026-09-18：完成 T6.4 统计页（概览 + SVG 趋势 + 60s 轮询）与 T8.3（统计入口、复制本站观看链接）。
- 2026-09-18：第一迭代验收；详情增加「复制开播链接」方便无摄像头机器把主持人页贴到笔记本。
- 2026-09-17：完成 T0.5 验证、T5 基础设施（axios 封装/路由/布局/API）与活动列表/创建/详情、观看页；两端引入 axios。
- 2026-09-17：迁入 `zhibo/zhibo-frontend`；改为只读 `zhibo-shared`。
