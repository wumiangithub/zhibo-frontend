# 前端模型协作说明

> **你只负责本仓库。** 用户是 10 年前端，页面可以一起打磨。不要打开 `D:\ai-project\qoder\zhibo`。

## 可以改

- `admin-web/**`
- `live-web/**`
- `docs/进度.md`（只记前端任务）

已定约束（当需求，不要改）：

- Vue 3 + TypeScript + Vite + UnoCSS
- `admin-web`：PC 管理端，端口 5173，`/api` → `8081`
- `live-web`：观看端，移动优先且 PC 可播，端口 5174，`/api` → `8082`（不要改回 h5-web）
- 后端返回 `{ code: 0, msg, data }`，`code === 0` 才成功
- **禁止**带微吼 `app_secret`，禁止直连 `saas-open.vhall.com`

## 不可以改

- 任何 `D:\ai-project\qoder\zhibo` 下的文件（后端 Java、架构文档、ADR）
- 目录名、端口、代理目标

缺接口：写在 `docs/进度.md`「阻塞项」，等后端补。不要 mock 一整套后端，也不要改 Java。

## 任务

| 编号 | 内容 |
|------|------|
| T0.3 / T0.4 / T0.5 | 脚手架补齐，UnoCSS 能跑 |
| T5.1 / T5.2 / T5.3 | Axios、路由、布局、API 封装 |
| T6.1～T6.4 | 管理端页面 + 观看页 |

做完更新 `docs/进度.md`，提交前缀 `[frontend]`。
