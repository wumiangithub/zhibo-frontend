# 前端协作

只改本仓库。用户是 10 年前端，页面可以一起打磨。

**先读** `../zhibo-shared/docs/项目背景.md` 和 `API约定.md`。

## 可以改

- `admin-web/**`
- `live-web/**`
- `docs/进度.md`

## 已定约束（细节以 shared 为准）

- Vue 3 + TS + Vite + UnoCSS
- `admin-web` 5173 → `/api` 8081；`live-web` 5174 → `/api` 8082
- 成功：`code === 0`
- 禁止直连微吼、禁止 `app_secret`
- 不要改回 h5-web

## 不可以改

- `../zhibo-backend`、`../zhibo-shared`
- 端口、代理目标、目录名

缺接口：写进度阻塞项，不要造后端。
