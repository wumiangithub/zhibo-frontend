# zhibo-frontend

Vue 前端。公用文档：`../zhibo-shared`。后端：`../zhibo-backend`。

```bat
cd admin-web && npm install && npm run dev
cd live-web && npm install && npm run dev
```

- 管理端 http://127.0.0.1:5173 → 8081
- 观看端 http://127.0.0.1:5174 → 8082
